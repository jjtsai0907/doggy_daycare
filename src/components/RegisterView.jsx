const RegisterView = ({nextScreen}) => {
    return (
        <div>
            This is Register View!
            <button onClick={ nextScreen }> Go to info</button>
        </div>
    ) 
}

export default RegisterView;